<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core">
    <SelectDialog
            noDataText="Kein Business-Partner gefunden"
            title="Wählen Sie einen Business-Partner"
            search="onSearch"
            confirm="onClose"
            cancel="onClose"
            items="{path: 'personal>data'}">
        <StandardListItem
                title="{personal>BusinessPartnerID}"
                description="{personal>CompanyName}"
                iconDensityAware="false"
                iconInset="false"
                type="Active" />
    </SelectDialog>
</core:FragmentDefinition>